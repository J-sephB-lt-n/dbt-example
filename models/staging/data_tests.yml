version: 2

models:
  - name: orders
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
          - is_valid_id # custom test defined in tests/generic/test_is_valid_id.sql
